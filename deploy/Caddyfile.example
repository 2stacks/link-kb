# Caddyfile for the production deploy (mounted by docker-compose.yml).
# Replace the placeholder domain below with your own. For local testing
# without a real domain, point the name at 127.0.0.1 in /etc/hosts.

kb.example.com {
	reverse_proxy link-kb:5000
	encode gzip
	log {
		output stdout
	}
}
